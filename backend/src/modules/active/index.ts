/**
 * HaiActive - Módulos para gimnasios y centros deportivos.
 * Servicios: miembros, acceso, planes, reservas de clases, entrenadores, etc.
 */
import { Router } from 'express'

const router = Router()

export const activeModule = router
